// React Query hooks for notes
// This file will contain hooks like useNotesQuery, useAddNote, etc.
// For now, it's a placeholder for future React Query implementation

export const useNotesQuery = () => {
  // Placeholder for future React Query implementation
  return {
    data: [],
    isLoading: false,
    error: null,
  };
};

export const useAddNote = () => {
  // Placeholder for future React Query implementation
  return {
    mutate: () => {},
    isLoading: false,
    error: null,
  };
};
